@model IEnumerable<CapaDatos.Models.DetalleXFactura>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Plato)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Valor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Factura)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Supervisor)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Plato)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Valor)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Factura.NroFactura)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Supervisor.Apellidos)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IdDetalleXFactura">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IdDetalleXFactura">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IdDetalleXFactura">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
